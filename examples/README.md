# Examples

Runnable demos linked from handbook topics live here.

Each example should use the matching topic id as its folder name, for example:

```text
examples/
  06-javascript.closures/
  10-react.useeffect/
```

Keep the docs site build independent of these packages. Link from topic pages only after an example is stable.
